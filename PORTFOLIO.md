# 나의 게시판 서비스

Next.js(App Router) + MongoDB 기반 게시판 프로젝트. 게시글 CRUD 같은 기본기를 다지기 위해 시작했고, "평범한 게시판을 사용자들이 더 자주, 더 오래 이용하게 하려면 무엇이 필요할까?"라는 질문을 계기로 실시간 소통 공간인 "만남의 광장"을 추가로 구현했습니다.

## 프로젝트 배경

- 회원가입/로그인, 게시글 작성·조회·수정·삭제, 댓글, 좋아요 등 게시판의 기본 CRUD와 인증 흐름을 처음부터 직접 구현하며 기본기를 다지기 위해 시작한 프로젝트입니다.
- 기능을 다 갖춘 뒤 "글 목록만 있는 평범한 게시판을 사용자가 더 잘 활용하게 하려면 무엇을 더할 수 있을까"를 고민했고, 그 답으로 사용자들이 실시간으로 모여 캐릭터를 움직이고 채팅할 수 있는 "만남의 광장"을 게시판에 추가했습니다.

## 주요 기능

- **인증**: JWT accessToken(15분)/refreshToken(30일) 발급·재발급, httpOnly 쿠키 기반 세션
- **게시판**: 글쓰기/수정/삭제(에디터, 이미지 업로드), 댓글, 좋아요, 검색·정렬·카테고리 필터, 실제 총 개수 기반 페이지네이션
- **마이페이지**: 작성 글/받은 좋아요/작성 댓글 통계, 내 활동 탭
- **만남의 광장**: 방향키로 캐릭터 이동, 말풍선 채팅, 여러 사용자 간 실시간(폴링 기반) 동기화
- **다크모드**: 디자인 토큰 기반으로 전체 화면에 일관되게 적용
- 직접 만든 컴포넌트 라이브러리를 npm에 배포하여 프로젝트에 연동

## 기술 스택

Next.js 16(App Router) · React 19 · TypeScript · Tailwind CSS v4 · MongoDB/Mongoose · JWT

## 기술적으로 신경 쓴 부분

- **서버리스 배포 환경에서도 동작하는 실시간 기능**: 웹소켓 대신 폴링 방식을 선택해, 별도의 상시 구동 서버 없이도 여러 사용자의 위치·채팅이 동기화되도록 설계. 클라이언트에서는 내 캐릭터 이동을 즉시 반영(로컬 예측)하고, 서버 상태는 일정 주기로 동기화. 접속이 끊긴 사용자는 TTL로 자동 정리.
- **일관된 다크모드**: 색상을 하드코딩하는 대신 CSS 변수 기반 디자인 토큰으로 라이트/다크 테마를 관리해, 새 화면을 추가해도 스타일이 어긋나지 않도록 구성.

## 트러블슈팅: 동시 접속자가 몰릴 때의 확장성

**문제 인식**

만남의 광장은 폴링 방식이라, 접속자 전원이 하나의 공간을 공유하면 다음 두 가지 문제가 있었습니다.

1. `GET /api/plaza/state`가 현재 접속 중인 인원 전체를 매 요청마다 반환하는 구조라, 동시 접속자 수(N)가 늘어날수록 트래픽이 N² 수준으로 증가함
2. 화면 크기는 고정인데 인원 제한이 없어서, 사람이 많아지면 서버 부하보다 먼저 아바타가 겹쳐 보이며 화면이 꽉 차버림

**해결책**

방 하나당 최대 인원(`ROOM_CAPACITY`)을 두고, 인원이 다 차면 다음 방으로 자동 배정되도록 "방 나누기(샤딩)"를 적용했습니다.

- `PlazaPresence`/`PlazaMessage`에 `room` 필드를 추가하고, 접속자 조회·채팅 로그를 항상 같은 방 사람들끼리만 주고받도록 격리
- 입장 시 `/api/plaza/join`에서 현재 각 방의 실시간 인원수(최근 15초 내 활동 기준)를 집계해, 정원이 안 찬 가장 앞 번호 방으로 자동 배정. 기존 방이 모두 차 있으면 다음 번호 방이 자동으로 새로 열림(방 개수 상한 없음)
- 이후 이동/채팅 요청에는 배정받은 방 번호를 그대로 실어 보내, 매 요청마다 다시 배정 로직을 타지 않도록 함
- 자동 배정 후에도 "다른 방도 가보고 싶을 것"이라는 피드백을 반영해, `/api/plaza/rooms`로 방별 실시간 인원수를 조회해 보여주고 원하는 방을 골라 이동할 수 있는 기능을 추가. 이동 요청 시에도 서버가 해당 방의 정원을 다시 검사해서, 꽉 찬 방으로는 이동할 수 없도록 함

**과정에서의 트레이드오프**

방 배정은 "현재 인원수를 세고 정원 미만이면 입장"하는 방식이라, 두 사용자가 거의 동시에 입장하면 아주 짧은 순간 정원을 1~2명 초과할 수 있는 경합 조건이 있습니다. 이를 완벽히 막으려면 원자적 카운터(예: 방별 카운터 문서에 조건부 `$inc`)가 필요하지만, 이 프로젝트 규모에서는 시각적으로 거의 티가 안 나는 수준이라 정확한 동시성 보장보다 구현 단순함을 택했습니다. 실제 서비스로 트래픽이 커진다면 이 부분을 원자적 카운터나 별도 큐로 개선할 여지가 있습니다.

## 코드 리뷰와 안정성 개선

기능을 다 만든 뒤, 정확성·보안·성능·유지보수성 기준으로 리뷰하도록 프롬프트를 정의해둔 리뷰 전용 서브에이전트로 이번에 작성한 변경분 전체(39개 파일, 약 2,400줄)를 리뷰받고 지적된 항목을 전부 반영했습니다.

**보안/정확성 관련**

- **방 정원 우회**: 정원 검사가 입장 API에서만 이루어져서, 이동·채팅 API를 UI 없이 직접 호출하면 꽉 찬 방에도 계속 들어갈 수 있었습니다. 방 이동이 실제로 일어나는 경우에만 정원을 재검사하도록 수정해, 정상적인 하트비트(같은 방 유지)는 추가 비용 없이 그대로 두면서 우회 경로를 막았습니다.
- **검색어 미이스케이프**: 게시글 검색어를 이스케이프 없이 MongoDB `$regex`에 그대로 넘기고 있어서, 특수문자가 섞인 입력이 성능 저하나 잘못된 매칭으로 이어질 여지가 있었습니다. 정규식 메타문자를 이스케이프한 뒤 검색하도록 수정했습니다.
- 그 외 서버 측 값 검증 누락(카테고리 화이트리스트, JSON 파싱 실패, 잘못된 페이지 번호) 등을 모두 방어 처리했습니다.

**React 상태/이펙트 관련**

- 방을 전환할 때마다 접속 종료 요청이 함께 나가버려서, 방금 새로 들어간 방에서 순간적으로 캐릭터가 사라졌다가 다시 나타나는 경합이 있었습니다. "접속 종료"는 실제로 페이지를 벗어날 때만 실행되도록 이펙트를 분리해 해결했습니다.

**트러블슈팅: 개발 서버가 통째로 멎던 문제**

리뷰에서 지적된 항목을 검증하던 도중, 개발 서버가 응답 없이 멈추고 컴퓨터 전체가 느려지는 현상을 실제로 재현했습니다. 원인을 추적해보니 Tailwind 설정에서 `node_modules` 바깥(패키지 소스 저장소가 있는 상위 폴더)을 참조하던 지시어 하나가, 번들러(Turbopack)의 모듈 해석 경로를 간헐적으로 오염시켜 이후 모든 요청이 무한정 응답하지 않는 상태로 빠지게 만들고 있었습니다. 해당 지시어를 제거하고, 필요한 클래스명은 프로젝트 내부 파일에 문자열로만 나열해두는 방식으로 바꿔 문제를 근본적으로 해결했습니다. 수정 후에는 위험한 패턴을 포함한 요청을 연속으로 보내도 항상 100ms 안팎으로 응답하는 것을 확인했습니다.

## Lighthouse 성능 개선

_(진행 예정)_ Lighthouse로 성능을 측정하고, 개선한 항목과 그 전후 점수를 이어서 정리할 예정입니다.
